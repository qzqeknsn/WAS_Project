<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - University Portal</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .hero-section {
            background: white;
            padding: 3rem 0;
            margin-bottom: 2rem;
            border-bottom: 1px solid #dee2e6;
        }

        .navbar {
            background-color: #000 !important;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/dashboard">
                <i class="bi bi-mortarboard-fill me-2"></i>UniPortal
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav align-items-center">
                    <li class="nav-item me-3">
                        <span class="text-white">Welcome, <strong>
                                <%= user.username %>
                            </strong></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <!-- IDOR Vulnerability Link -->
                        <a class="nav-link" href="/grades?student_id=<%= user.id %>">My Grades</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-outline-light btn-sm rounded-0" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero / Search Section -->
    <div class="hero-section">
        <div class="container text-center">
            <h1 class="display-5 mb-4 fw-bold">Course Search</h1>
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">
                    <form action="/dashboard" method="GET" class="d-flex gap-2">
                        <input type="text" name="q" class="form-control form-control-lg rounded-0"
                            placeholder="Search for subjects..." value="<%= query || '' %>">
                        <button type="submit" class="btn btn-dark btn-lg px-4 rounded-0">Search</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Results Section -->
    <div class="container">
        <% if (query) { %>
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="card shadow-sm border-0 rounded-0">
                        <div class="card-header bg-white border-bottom-0 pt-4">
                            <h5 class="card-title text-dark fw-bold"><i class="bi bi-search me-2"></i>Search Results
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="alert alert-light border rounded-0">
                                <!-- SECURITY FIX: Reflected XSS -->
                                <!-- We swapped <%- %> (raw) for <%= %> (escaped). Scripts will now render as text. -->
                                Results for: <strong>
                                    <%= query %>
                                </strong>
                            </div>
                            <p class="text-muted text-center py-4">
                                <i class="bi bi-inbox display-4 d-block mb-3 opacity-25"></i>
                                No active courses found matching your criteria (Demo Mode).
                            </p>
                        </div>
                    </div>
                </div>
            </div>
            <% } else { %>
                <div class="text-center text-muted mt-5">
                    <p>Use the search bar above to find your courses.</p>
                </div>
                <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>