<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Corporate Portal</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #0d1117;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Courier New', Courier, monospace;
        }

        .card {
            border: 1px solid #30363d;
            background-color: #161b22;
        }

        .text-green {
            color: #2ea043;
        }

        .btn-green {
            background-color: #238636;
            color: white;
            border: none;
        }

        .btn-green:hover {
            background-color: #2ea043;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-4">
                <div class="card shadow-lg">
                    <div class="card-body p-5">
                        <h3 class="text-center mb-4 text-green">&gt; AUTHENTICATE_</h3>

                        <% if (error) { %>
                            <div class="alert alert-danger font-monospace" role="alert">
                                [!] <%= error %>
                            </div>
                            <% } %>

                                <form action="/login" method="POST">
                                    <div class="mb-3">
                                        <label for="username" class="form-label text-muted">&gt; USERNAME</label>
                                        <input type="text" class="form-control font-monospace bg-dark" id="username"
                                            name="username" required>
                                    </div>

                                    <div class="mb-3">
                                        <label for="password" class="form-label text-muted">&gt; PASSWORD</label>
                                        <input type="password" class="form-control font-monospace bg-dark" id="password"
                                            name="password" required>
                                    </div>

                                    <div class="d-grid gap-2 mt-4">
                                        <button type="submit" class="btn btn-green btn-lg font-monospace">ACCESS
                                            SYSTEM</button>
                                    </div>
                                </form>
                    </div>
                    <div class="card-footer text-center border-top border-secondary">
                        <small class="text-secondary font-monospace">SECURE TERMINAL V.3.0</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>