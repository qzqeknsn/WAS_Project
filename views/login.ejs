<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - CyberState University</title>
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            height: 100vh;
            overflow: hidden;
        }

        .split-screen {
            display: flex;
            height: 100%;
        }

        .left-pane {
            flex: 1;
            background: url('https://images.unsplash.com/photo-1541339907198-e08756dedf3f?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80') no-repeat center center/cover;
            position: relative;
        }

        .left-pane::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(13, 110, 253, 0.8), rgba(13, 202, 240, 0.6));
        }

        .left-content {
            position: absolute;
            bottom: 50px;
            left: 50px;
            color: white;
            z-index: 2;
            max-width: 500px;
        }

        .right-pane {
            width: 500px;
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
        }

        .login-card {
            width: 100%;
            max-width: 380px;
        }

        .logo-icon {
            font-size: 2.5rem;
            color: #0d6efd;
            margin-bottom: 1rem;
        }

        .form-control {
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
        }

        .form-control:focus {
            background-color: #fff;
            box-shadow: 0 0 0 4px rgba(13, 110, 253, 0.1);
        }

        .btn-primary {
            padding: 0.75rem;
            border-radius: 0.5rem;
            font-weight: 600;
            background: linear-gradient(to right, #0d6efd, #0099ff);
            border: none;
        }

        .btn-primary:hover {
            box-shadow: 0 4px 12px rgba(13, 110, 253, 0.3);
            transform: translateY(-1px);
        }
    </style>
</head>

<body>
    <div class="split-screen">
        <div class="left-pane d-none d-lg-block">
            <div class="left-content">
                <h1 class="display-4 fw-bold mb-3">Welcome Future Leaders</h1>
                <p class="lead mb-0">Join the world's most advanced secure systems program. Access your grades, campus
                    news, and research portals.</p>
            </div>
        </div>

        <div class="right-pane">
            <div class="login-card">
                <div class="text-center mb-5">
                    <i class="fa-solid fa-shield-halved logo-icon"></i>
                    <h2 class="fw-bold text-dark">CyberState Univ.</h2>
                    <p class="text-muted">Secure Student Portal Access</p>
                </div>

                <% if (error) { %>
                    <div class="alert alert-danger d-flex align-items-center shadow-sm border-0" role="alert">
                        <i class="fa-solid fa-circle-exclamation me-2"></i>
                        <div>
                            <%= error %>
                        </div>
                    </div>
                    <% } %>

                        <form action="/login" method="POST">
                            <div class="mb-3">
                                <label class="form-label text-xs fw-bold text-uppercase text-secondary small">Username /
                                    Student ID</label>
                                <div class="input-group">
                                    <span class="input-group-text border-0 bg-light text-muted"><i
                                            class="fa-solid fa-user"></i></span>
                                    <input type="text" class="form-control border-start-0 ps-0 text-muted"
                                        name="username" placeholder="Type your username" required>
                                </div>
                            </div>

                            <div class="mb-4">
                                <div class="d-flex justify-content-between">
                                    <label
                                        class="form-label text-xs fw-bold text-uppercase text-secondary small">Password</label>
                                    <a href="#" class="small text-decoration-none">Forgot?</a>
                                </div>
                                <div class="input-group">
                                    <span class="input-group-text border-0 bg-light text-muted"><i
                                            class="fa-solid fa-lock"></i></span>
                                    <input type="password" class="form-control border-start-0 ps-0 text-muted"
                                        name="password" placeholder="••••••••" required>
                                </div>
                            </div>

                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary">
                                    Sign In <i class="fa-solid fa-arrow-right ms-2"></i>
                                </button>
                            </div>
                        </form>

                        <div class="mt-5 text-center">
                            <p class="small text-muted mb-0">System Status: <span class="text-success"><i
                                        class="fa-solid fa-circle fa-2xs"></i> Operational</span></p>
                            <p class="small text-muted opacity-50">&copy; 2025 Secure Systems. All rights reserved.</p>
                        </div>
            </div>
        </div>
    </div>
</body>

</html>